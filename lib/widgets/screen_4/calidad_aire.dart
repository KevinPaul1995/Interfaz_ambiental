import 'package:flutter/material.dart';

class CalidadAire extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Pantalla de Calidad de Aire',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
